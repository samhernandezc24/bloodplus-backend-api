from django.urls import path
from .views import user

urlpatterns = [
    #path('', views.getRoutes),
    #path('users/', user_views.register, name='register'),
    #path('me/', user_views.currentUser, name='current_user'),

    #path('token/', user_views)
]
